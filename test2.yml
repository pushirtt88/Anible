---
 - name: Please Install the packages
   hosts: all
   gather_facts: false
   vars:
    linux: http
    ubuntu: apache
   tasks:
   - name: Please find the openssl  version 
     
      
          